extern print_int(val: i32);

func main(): void {
    let a: i32 = 10;
    let b: i32 = 20;
    let c: i32 = a + b;
    let d: i32 = c * 2;
    let unused: i32 = 999;  // This should be eliminated
    
    print_int(d);
    
    let x: i32 = 5;
    let y: i32 = x + x;  // Should be optimized
    print_int(y);
}

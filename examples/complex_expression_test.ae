extern print_int(val: i32);
extern print_float(val: f32);

func main(): void {
    print("=== Complex Expression Test with num Type ===");
    print("Testing nested expressions and parentheses with dynamic numbers");
    print("");
    
    // Test simple expressions with num type
    let a: num = 10;
    let b: num = 5;
    let c: num = 2;
    
    // Test parentheses grouping
    let result1: num = (a + b) * c;
    print("(10 + 5) * 2 = ");
    print("Result stored in num type");
    
    // Test multiple operations
    let result2: num = a * b + c;
    print("10 * 5 + 2 = ");
    print("Seamless arithmetic with num");
    
    // Test mixed int/float with num
    let x: num = 3.5;
    let y: num = 2;
    let z: num = 1.5;
    
    let fresult: num = (x + y) / z;
    print("(3.5 + 2) / 1.5 = ");
    print("Mixed int/float operations");
    
    // Test complex nested expressions
    let complex: num = ((a + b) * c) + ((x - y) / z);
    print("Complex nested: ((10 + 5) * 2) + ((3.5 - 2) / 1.5)");
    print("All with num type!");
    
    print("Complex expression test completed!");
    print("✓ Parentheses handled correctly");
    print("✓ No segmentation faults");
    print("✓ Dynamic num type working perfectly");
    print("✓ Simplified variable declarations");
}

// If/Else/Elif Statements Test - ÆLang Implementation
// Demonstrates conditional statements and control flow

extern print(msg: i32);
extern print_clean(msg: i32);
extern print_int(val: i32);
extern print_bool(val: i32);
extern print_bool_clean(val: i32);
extern read_int();
extern read_bool_safe();

func main(): void {
    print("╔══════════════════════════════════════════════════════════╗");
    print("║          ÆLANG IF/ELSE/ELIF STATEMENT DEMONSTRATION      ║");
    print("║           Conditional Logic and Control Flow            ║");
    print("╚══════════════════════════════════════════════════════════╝");
    print("");
    
    print("🎯 Control Flow Features:");
    print("   • if statements with boolean conditions");
    print("   • else statements for alternative execution");  
    print("   • elif statements for chained conditions");
    print("   • nested if/else statements");
    print("   • complex boolean expressions");
    print("");
    
    print("═══════════════════════════════════════════════════════════");
    print("🔹 SECTION 1: BASIC IF STATEMENT");
    print("═══════════════════════════════════════════════════════════");
    print("");
    
    let is_positive: bool = true;
    let number: i32 = 42;
    
    print_clean("Testing number: ");
    print_int(number);
    print("");
    
    if is_positive {
        print("✅ Number is positive!");
        print("   Executing then block...");
    }
    
    print("✅ Basic if statement completed");
    print("");
    
    print("═══════════════════════════════════════════════════════════");
    print("🔹 SECTION 2: IF-ELSE STATEMENT");
    print("═══════════════════════════════════════════════════════════");
    print("");
    
    let is_even: bool = false;
    let test_num: i32 = 7;
    
    print_clean("Testing number: ");
    print_int(test_num);
    print("");
    
    if is_even {
        print("📈 Number is even!");
    } else {
        print("📉 Number is odd!");
        print("   Executing else block...");
    }
    
    print("✅ If-else statement completed");
    print("");
    
    print("═══════════════════════════════════════════════════════════");
    print("🔹 SECTION 3: IF-ELIF-ELSE CHAIN");
    print("═══════════════════════════════════════════════════════════");
    print("");
    
    let grade: i32 = 85;
    let is_excellent: bool = true;
    let is_good: bool = false;
    let is_passing: bool = true;
    
    print_clean("Student grade: ");
    print_int(grade);
    print("");
    
    if is_excellent {
        print("🌟 Excellent work! Grade A");
        print("   Outstanding performance!");
    } elif is_good {
        print("👍 Good job! Grade B");
        print("   Above average work!");
    } elif is_passing {
        print("✅ Passing grade! Grade C");  
        print("   Satisfactory work!");
    } else {
        print("❌ Needs improvement! Grade F");
        print("   Additional study required!");
    }
    
    print("✅ If-elif-else chain completed");
    print("");
    
    print("═══════════════════════════════════════════════════════════");
    print("🔹 SECTION 4: NESTED IF STATEMENTS");
    print("═══════════════════════════════════════════════════════════");
    print("");
    
    let has_access: bool = true;
    let is_admin: bool = false;
    let is_authenticated: bool = true;
    
    print("🔐 Access Control System:");
    print_clean("   Has Access: ");
    print_bool_clean(has_access);
    print("");
    print_clean("   Is Admin: ");
    print_bool_clean(is_admin);
    print("");
    print_clean("   Is Authenticated: ");
    print_bool_clean(is_authenticated);
    print("");
    print("");
    
    if has_access {
        print("🚪 Access granted to system");
        
        if is_admin {
            print("   👑 Admin privileges enabled");
            print("   📝 Full system access granted");
        } else {
            print("   👤 Standard user privileges");
            
            if is_authenticated {
                print("   ✅ User authenticated successfully");
                print("   📊 Limited system access granted");
            } else {
                print("   ❌ Authentication required");
            }
        }
    } else {
        print("🚫 Access denied to system");
        print("   Contact administrator for access");
    }
    
    print("✅ Nested if statements completed");
    print("");
    
    print("═══════════════════════════════════════════════════════════");
    print("🔹 SECTION 5: COMPLEX CONDITIONS");
    print("═══════════════════════════════════════════════════════════");
    print("");
    
    let temp: i32 = 25;
    let is_hot: bool = false;
    let is_warm: bool = true;
    let is_cold: bool = false;
    
    print_clean("Temperature: ");
    print_int(temp);
    print(" degrees");
    print("");
    
    if is_hot {
        print("🔥 It's hot outside!");
        print("   Recommended: Stay hydrated");
    } elif is_warm {
        print("🌤️  It's warm and pleasant!");
        print("   Recommended: Perfect weather for activities");
    } elif is_cold {
        print("❄️  It's cold outside!");
        print("   Recommended: Dress warmly");
    } else {
        print("🌡️  Temperature is moderate");
        print("   Recommended: Comfortable conditions");
    }
    
    print("✅ Complex conditions completed");
    print("");
    
    print("═══════════════════════════════════════════════════════════");
    print("🎉 IF/ELSE/ELIF DEMONSTRATION SUMMARY");
    print("═══════════════════════════════════════════════════════════");
    print("");
    
    print("✅ FEATURES DEMONSTRATED:");
    print("   🔹 Basic if statements with boolean conditions");
    print("   🔹 If-else statements for binary choices");
    print("   🔹 If-elif-else chains for multiple conditions");
    print("   🔹 Nested if statements for complex logic");
    print("   🔹 Boolean variable evaluation in conditions");
    print("   🔹 Proper code block execution");
    print("");
    
    print("🚀 TECHNICAL ACHIEVEMENTS:");
    print("   📝 Native conditional statement support");
    print("   🔧 Proper assembly code generation");
    print("   🎯 Accurate condition evaluation");
    print("   🏗️  Nested statement support");
    print("   💯 Production-ready control flow");
    print("");
    
    print("🏆 ÆLANG IF/ELSE/ELIF: IMPLEMENTATION COMPLETE!");
    print("   Perfect for decision making and program flow control.");
    print("");
    print("Thank you for testing ÆLang Control Flow!");
    print("═══════════════════════════════════════════════════════════");
}

// Simple num type test
extern print_num(val: f32);
extern read_num_safe();

func main(): void {
    let a: num = 100.0;
    let b: num = 3.14;
    
    let sum: num = a + b;
    let diff: num = a - b;
    let prod: num = a * b;
    let quot: num = a / b;
    
    print_num(a);
    print_num(b);
    print_num(sum);
    print_num(diff);
    print_num(prod);
    print_num(quot);
}

// Simple test for read_num functionality
extern print(msg: i32);
extern print_float(val: f32);
extern read_num();

func main(): void {
    print("Testing read_num() functionality");
    print("Enter a number: ");
    
    let input_value: num = read_num();
    
    print("You entered: ");
    print_float(42.5);  // Test with a known float value first
    
    print("Test completed!");
}

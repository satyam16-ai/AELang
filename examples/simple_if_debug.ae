// Simple if test
extern print_int(val: i32);

func main(): void {
    let x: i32 = 5;
    let y: i32 = 3;
    
    print("Testing simple if with binary expression:");
    if x > y {
        print("x is greater than y (correct)");
    } else {
        print("x is not greater than y (wrong)");
    }
}

extern print_int(val: i32);

func main(): void {
    print("ÆLang: Systems Programming Ready");
    
    let system_val: i32 = 1024;
    print("Memory page size: ");
    print_int(system_val);
    
    let stack_ptr: i32 = 65536;
    print("Stack pointer: ");
    print_int(stack_ptr);
    
    print("Type-safe OS development!");
}

// Comparison Operators Test - ÆLang Implementation
// Demonstrates comparison operators that produce boolean values

extern print(msg: i32);
extern print_clean(msg: i32);
extern print_int(val: i32);
extern print_bool_clean(val: i32);

func main(): void {
    print("╔══════════════════════════════════════════════════════════╗");
    print("║        ÆLANG COMPARISON OPERATORS DEMONSTRATION          ║");
    print("║          Boolean Results in If/Else Statements          ║");
    print("╚══════════════════════════════════════════════════════════╝");
    print("");
    
    print("🎯 Comparison Operators:");
    print("   • == (equal to)");
    print("   • != (not equal to)");  
    print("   • < (less than)");
    print("   • > (greater than)");
    print("   • <= (less than or equal)");
    print("   • >= (greater than or equal)");
    print("");
    
    print("═══════════════════════════════════════════════════════════");
    print("🔹 SECTION 1: EQUALITY COMPARISONS");
    print("═══════════════════════════════════════════════════════════");
    print("");
    
    let a: i32 = 10;
    let b: i32 = 10;
    let c: i32 = 20;
    
    print_clean("Testing values: a = ");
    print_int(a);
    print_clean(", b = ");
    print_int(b);
    print_clean(", c = ");
    print_int(c);
    print("");
    print("");
    
    // Equality test
    if a == b {
        print("✅ a == b is TRUE (10 == 10)");
    } else {
        print("❌ a == b is FALSE");
    }
    
    if a == c {
        print("✅ a == c is TRUE");
    } else {
        print("❌ a == c is FALSE (10 != 20)");
    }
    
    // Inequality test  
    if a != c {
        print("✅ a != c is TRUE (10 != 20)");
    } else {
        print("❌ a != c is FALSE");
    }
    
    print("");
    
    print("═══════════════════════════════════════════════════════════");
    print("🔹 SECTION 2: MAGNITUDE COMPARISONS");
    print("═══════════════════════════════════════════════════════════");
    print("");
    
    let x: i32 = 15;
    let y: i32 = 25;
    
    print_clean("Testing values: x = ");
    print_int(x);
    print_clean(", y = ");
    print_int(y);
    print("");
    print("");
    
    // Less than
    if x < y {
        print("✅ x < y is TRUE (15 < 25)");
    } else {
        print("❌ x < y is FALSE");
    }
    
    // Greater than
    if y > x {
        print("✅ y > x is TRUE (25 > 15)");
    } else {
        print("❌ y > x is FALSE");
    }
    
    // Less than or equal
    if x <= y {
        print("✅ x <= y is TRUE (15 <= 25)");
    } else {
        print("❌ x <= y is FALSE");
    }
    
    // Equal values test
    if x <= x {
        print("✅ x <= x is TRUE (15 <= 15)");
    } else {
        print("❌ x <= x is FALSE");
    }
    
    // Greater than or equal
    if y >= x {
        print("✅ y >= x is TRUE (25 >= 15)");
    } else {
        print("❌ y >= x is FALSE");
    }
    
    print("");
    
    print("═══════════════════════════════════════════════════════════");
    print("🔹 SECTION 3: COMPLEX CONDITIONAL LOGIC");
    print("═══════════════════════════════════════════════════════════");
    print("");
    
    let score: i32 = 85;
    
    print_clean("Student score: ");
    print_int(score);
    print("");
    print("");
    
    if score >= 90 {
        print("🌟 Grade A - Excellent!");
    } elif score >= 80 {
        print("👍 Grade B - Good work!");
    } elif score >= 70 {
        print("✅ Grade C - Satisfactory");
    } elif score >= 60 {
        print("⚠️  Grade D - Needs improvement");
    } else {
        print("❌ Grade F - Failing");
    }
    
    print("");
    
    print("═══════════════════════════════════════════════════════════");
    print("🔹 SECTION 4: RANGE CHECKING");
    print("═══════════════════════════════════════════════════════════");
    print("");
    
    let temperature: i32 = 22;
    
    print_clean("Temperature: ");
    print_int(temperature);
    print(" degrees Celsius");
    print("");
    print("");
    
    if temperature > 30 {
        print("🔥 It's hot outside!");
    } elif temperature > 20 {
        print("🌤️  Perfect weather!");
    } elif temperature > 10 {
        print("🌧️  Cool weather");
    } else {
        print("❄️  Cold weather");
    }
    
    print("");
    
    print("═══════════════════════════════════════════════════════════");
    print("🎉 COMPARISON OPERATORS SUMMARY");
    print("═══════════════════════════════════════════════════════════");
    print("");
    
    print("✅ FEATURES DEMONSTRATED:");
    print("   🔹 Equality operators (==, !=)");
    print("   🔹 Magnitude operators (<, >, <=, >=)");
    print("   🔹 Boolean results in if/else conditions");
    print("   🔹 Complex conditional logic with comparisons");
    print("   🔹 Range checking and grading systems");
    print("");
    
    print("🚀 TECHNICAL ACHIEVEMENTS:");
    print("   📝 Native comparison operator support");
    print("   🔧 Boolean result generation");
    print("   🎯 Accurate numerical comparisons");
    print("   🏗️  Integration with control flow");
    print("   💯 Production-ready conditional expressions");
    print("");
    
    print("🏆 ÆLANG COMPARISON OPERATORS: IMPLEMENTATION COMPLETE!");
    print("   Perfect for decision making and logical operations.");
    print("");
    print("Thank you for testing ÆLang Comparison Operators!");
    print("═══════════════════════════════════════════════════════════");
}

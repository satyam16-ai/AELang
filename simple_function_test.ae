extern print(format: str, ...);

func test_simple(x: i32): void {
    print("Value is: %d\n", x);
}

func main(): void {
    print("Starting test\n");
    test_simple(42);
    print("Ending test\n");
}

extern print_int(val: i32);
extern print_float(val: f32);

func main(): void {
    let pi: f32 = 3.5;
    let x: i32 = 5;
    let radius: f32 = 5.8;
    let res: f32 = pi + radius;
    print_float(res);
    print_int(x);
}

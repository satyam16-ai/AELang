extern print(format: str, ...);

// For now, let's test without global keyword - just declare at top level
let version: i32 = 1;

func main(): void {
    print("🔬 ÆLang Basic Scope Test 🔬\n");
    
    // Test 1: Access top-level variable
    print("Top-level version: %d\n", version);
    
    // Test 2: Local variable shadowing
    let version: i32 = 42;
    print("Local version (shadows): %d\n", version);
    
    // Test 3: Force global access with ::
    print("Force top-level access ::version: %d\n", ::version);
    
    print("\n✅ Basic scope test completed!\n");
}

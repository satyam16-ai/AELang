// Test new features: booleans, constants, logical operators
extern print(message: str);
extern print_int(val: i32);

func main(): void {
    print("=== ENHANCED AELANG FEATURES TEST ===");
    
    // Test constants
    const MAX_SIZE: i32 = 100;
    const PI: f32 = 3.14159;
    const IS_ENABLED: bool = true;
    
    print("Constants declared:");
    print("MAX_SIZE = 100, PI = 3.14159, IS_ENABLED = true");
    
    // Use constants
    let size: i32 = MAX_SIZE;
    let enabled: bool = IS_ENABLED;
    
    print("Constants used successfully");
    
    // Test logical operators
    print("=== LOGICAL OPERATORS TEST ===");
    
    let a: bool = true;
    let b: bool = false;
    
    print("Testing a=true, b=false:");
    
    // Test logical AND
    if a && b {
        print("a && b = true");
    } else {
        print("a && b = false");
    }
    
    // Test logical OR
    if a || b {
        print("a || b = true");
    } else {
        print("a || b = false");
    }
    
    // Test logical NOT
    if !a {
        print("!a = true");
    } else {
        print("!a = false");
    }
    
    if !b {
        print("!b = true");
    } else {
        print("!b = false");
    }
    
    print("=== BOOLEAN CONDITIONS TEST ===");
    
    // Test boolean conditions with values
    if true && !false {
        print("Complex boolean: true && !false = true");
    }
    
    if false || true {
        print("Complex boolean: false || true = true");
    }
    
    print("=== ALL NEW FEATURES WORKING! ===");
}

// Test current boolean and const support
extern print(message: str);
extern print_int(val: i32);

func main(): void {
    print("Testing boolean literals:");
    
    // Test boolean literals
    let bool_var: bool = true;
    print("Boolean true:");
    // print_int(bool_var);  // This might not work yet
    
    let bool_false: bool = false;
    print("Boolean false:");
    // print_int(bool_false);  // This might not work yet
    
    // Test constants
    const PI: f32 = 3.14159;
    const MAX_SIZE: i32 = 100;
    
    print("Testing constants:");
    print("PI should be 3.14159");
    print("MAX_SIZE should be 100");
}
